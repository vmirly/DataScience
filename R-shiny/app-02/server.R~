## server.R 
## example for user input and output:

library(shiny)

makeRandomDF <- function(n=1) {
	
}

shinyServer(
    function(input, output) {
	output$outputId1 <- renderPrint({input$id1})
	output$outputId2 <- renderPrint({input$id2})

	output$outputId3 <- renderPrint({input$id1*40})
    }
)
